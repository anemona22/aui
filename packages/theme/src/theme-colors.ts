import { ThemeColors } from "./types";

import { blue, gray, green, red, amber, indigo } from "@ideoui/colors";

const THEME_COLORS: ThemeColors = {
  primary: blue[500],
  secondary: gray[600],
  success: green[800],
  danger: red[500],
  warning: amber[500],
  info: indigo[500],
};

export default THEME_COLORS;
